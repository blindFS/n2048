#include <iostream>
#include <vector>
#include <cstdlib>

#ifndef BRICK_H
#define BRICK_H

class brick {
    public:
        int x, y, number;
        brick(int x, int y, int number);
        brick();
};

#endif
